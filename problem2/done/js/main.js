// main.js

function displayPlans(elementSelector, plansData) {
  
  // Possible function to implement that 
  // will generate the desired HTML based on the JSON object, plansData, and
  // push the resulting HTML into the element, elementSelector.
  
}

